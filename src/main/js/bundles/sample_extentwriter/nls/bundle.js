define({
    root:
    {
        bundleName: "Extent Writer",
        bundleDescription: "This bundle writes the current map extent.",
        ui : {
            extentTemplate : "Extent: ${extent.xmin}; ${extent.ymin} - ${extent.xmax}; ${extent.ymax}"
        }
    },
    de:true
});